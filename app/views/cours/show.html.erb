<div class="container">
  <h1><%= @cours.name %></h1>
  <p>Niveau : <%= @cours.level %></p>
  <p><%= @cours.description %></p>
  <%= link_to "télécharger", download_path(@cours.file), target: "_blank" %>

  <%= link_to "Modifier", edit_cour_path(params[:id]) %>
</div>
